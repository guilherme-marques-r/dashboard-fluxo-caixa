# Segredos do dashboard (produção).
# Copie este arquivo para o painel de "Secrets" do Streamlit Cloud.
#
# Senha única para visualizar o dashboard. Se vazio/vazio, o dashboard abre sem senha.
senha_dashboard = ""

# Opcional: sobrescrever a planilha utilizada (padrão: lê do bot/.env)
# planilha_id = ""